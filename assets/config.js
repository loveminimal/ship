const baseURL = 'aituyaa.com';
const config = {
	// Set default engine, includes
	// - by: bing
	// - go: google - a piece of 404 cloud
	// - bd: baidu
	engine: 'by',
	// Set frequents nav
	nav: {
		Aituyaa: `//${baseURL}`,
		Nav: `//${baseURL}/nav`,
		Wiki: `//${baseURL}/wiki`,
		Mine: `//${baseURL}/about`,
		':-)':`//${baseURL}/emojing`,
	}
}